# zsh config
setopt +o nomatch
setopt auto_pushd
setopt pushd_ignore_dups

. "$ZDOTDIR/.aliases"

# mise
eval "$(~/.local/bin/mise activate zsh)"

# starship
eval "$(starship init zsh)"

# sheldon
eval "$(sheldon source)"

# rust
. "$HOME/.cargo/env"